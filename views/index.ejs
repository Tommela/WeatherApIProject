<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
</head>
<body>
    <div class=" h-screen flex justify-center items-center ">
        <div class=" w-[500px] shadow-2xl rounded-xl overflow-hidden px-5 py-10">
            <h1 class=" text-3xl">
                Open Weather App
            </h1>
            <img class="w-100" src="imgs/weather.jpg" alt="">
             <% if(locals.content) { %>
           <div class=" my-5">
             <p> Weather : <%= content.weather[0].main   %>   </p>
    
            <p> Weather Description : <%= content.weather[0].description %> </p>
           </div>
            <% } else { %>
            <div class=" my-5 text-slate-600">
                <p class="text-slate-600"> Fill the target city name to get weather condition </p>
                <p>Weather : ...</p>
                <p>Weather Description : ...</p>
            </div>
            <% } %>
            <form action="" method="post">
                <input class="mt-1 px-3 py-2 bg-white border shadow-sm border-slate-300 placeholder-slate-400 focus:outline-none focus:border-sky-500 focus:ring-sky-500 block w-full rounded-md sm:text-sm focus:ring-1"
                type="text" name="cityName" placeholder="enter city name">
                <div class=" flex mt-8 justify-center">
                    <button class=" border bg-red-500 text-white px-28 py-1 rounded-2xl" formaction="/submit">Click Me</button>
                </div>
            </form>
        </div>
        
       
    </div>
    
</body>
</html>